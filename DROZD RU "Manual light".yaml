blueprint:
  name: DROZD RU "Manual light"
  description: Попрощайтесь со светом, который забыли выключить! Переходите к автоматизированному освещению с автоотключением и сбросом таймера по датчику движения. 🪄💡\n\n**Если вам нравятся мои «blueprints», и вы хотите поддержать или просто сказать спасибо?** [Нажмите здесь](https://www.paypal.com/donate/?hosted_button_id=WAZS3QSDTPGA8) 🙂\n\n<details> <summary><b>Процесс автоматизации:</b> 👇</summary>\n\n\n  - **Триггер:**\n    - Когда сущность включается вручную (ON), она автоматически выключается после заданной задержки.\n\n  - **Опция синхронизации триггера:**\n    - При использовании нескольких сущностей: если одна меняет состояние (вручную включили или выключили), все сущности автоматически синхронизируют своё состояние.\n\n  - **Опция датчика движения:**\n    - Когда свет включён вручную и обнаружено движение, таймер ставится на паузу.\n    - Как только движение прекращается, таймер сбрасывается на начало.\n    - Если вы не используете таймер helper, задержка заново запускается после окончания движения.\n\n  - **Льготный период (Grace Period):**\n    - При использовании датчика движения, если свет выключился автоматически, короткий льготный период позволяет ему включиться автоматически при повторном обнаружении движения.\n    - По окончании льготного периода свет нужно включать вручную.\n    - **Примечание:** это не применяется, если свет выключен вручную — ручное выключение всегда имеет приоритет.\n\n  - **Обход (Bypass):**\n    - Используйте опцию обхода, чтобы временно отключить автоматизацию и управлять светом вручную.\n\n  - **Поведение при перезапуске HA:**\n    - С таймером helper:\n      - Возобновится после перезапуска Home Assistant, сохранив состояние таймера.\n    - Без таймера helper:\n      - После перезапуска Home Assistant автоматизация попытается восстановиться, запустив задержку заново с начала.\n</details>\n\nНужна помощь?\nFAQ: [Нажмите здесь](https://community.home-assistant.io/t/688273/2)\nПоддержка сообщества, включая обновления: [Нажмите здесь](https://community.home-assistant.io/t/688273/1)\n\nТребуется = *\n"
  domain: automation
  input:
    trigger:
      name: Триггер *
      icon: mdi:cog-outline
      collapsed: true
      input:
        trigger_input:
          name: Триггер — Свет и выключатели *
          description: "Выберите сущности, которые будут автоматически выключаться после настроенной задержки.\n\n**Примечание** — Все выбранные сущности должны поддерживать состояния ON и OFF.\n"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - light
                - switch
              multiple: true
              reorder: false
        time_delay:
          name: Задержка времени
          description: "Укажите, как долго свет и выключатели остаются включёнными перед автоматическим выключением.\n\nЕсли используется датчик движения, таймер сбрасывается при каждом обнаружении движения. Когда движения нет и задержка истекает, свет и выключатели выключаются.\n"
          default:
            days: 0
            hours: 0
            minutes: 5
            seconds: 0
          selector:
            duration: {}
        include_timer_helper:
          name: Использовать опцию таймера helper (опционально)
          description: "Включите, если хотите использовать таймер helper.\n\n**Примечание** — При создании таймера helper отметьте флажок `Restore`, чтобы он сохранялся после перезапуска Home Assistant. Не задавайте длительность в самом таймере helper; время контролируется настройками ниже.\n\nПодробнее о создании таймера helper — [нажмите здесь](https://community.home-assistant.io/t/688273/4).\n"
          default: disable_timer_helper
          selector:
            select:
              options:
              - label: Включить опцию таймера helper
                value: enable_timer_helper
              - label: Отключить опцию таймера helper
                value: disable_timer_helper
              sort: false
              multiple: false
              custom_value: false
        timer_helper:
          name: Таймер helper
          description: "Выберите таймер helper для использования, если опция таймера helper включена выше.\n"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - timer
              multiple: false
              reorder: false
        include_trigger_sync:
          name: Использовать опцию синхронизации (опционально)
          description: "Включите эту опцию, чтобы синхронизировать состояния ON/OFF всех выбранных сущностей. Если выбрано более одной сущности, они будут автоматически включаться и выключаться вместе.\n"
          default: disable_trigger_sync
          selector:
            select:
              options:
              - label: Включить опцию синхронизации
                value: enable_trigger_sync
              - label: Отключить опцию синхронизации
                value: disable_trigger_sync
              sort: false
              multiple: false
              custom_value: false
    motion:
      name: Датчик движения
      icon: mdi:motion-sensor
      collapsed: true
      input:
        include_motion_trigger:
          name: Использовать опцию датчика движения (опционально)
          description: "Включите эту опцию, чтобы использовать датчик движения. Независимо от того, используете ли вы таймер helper или встроенную задержку Home Assistant, таймер/задержка будет на паузе, пока обнаруживается движение, и сбрасываться после прекращения движения.\n"
          default: disable_motion_trigger
          selector:
            select:
              options:
              - label: Включить опцию датчика движения
                value: enable_motion_trigger
              - label: Отключить опцию датчика движения
                value: disable_motion_trigger
              sort: false
              multiple: false
              custom_value: false
        motion_trigger:
          name: Датчик движения — бинарные сенсоры
          description: "Выберите один датчик движения или бинарный сенсор. Если нужно использовать несколько сенсоров, сначала сгруппируйте их через group helper. Это обеспечивает более стабильное выполнение автоматизации и помогает избежать конфликтов.\n\nПодробнее о группировании сенсоров-триггеров — [нажмите здесь](https://community.home-assistant.io/t/688273/3).\n"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - binary_sensor
              multiple: false
              reorder: false
    grace_period:
      name: Льготный период
      icon: mdi:timer-outline
      collapsed: true
      input:
        include_grace_period:
          name: Использовать опцию льготного периода (опционально)
          description: "Включите короткий льготный период после выключения света из-за отсутствия движения. Если движение обнаружено в этот период, свет включится автоматически. После окончания льготного периода свет необходимо включать вручную.\n\n**Примечание** — Эта опция работает только при включённой опции датчика движения.\n"
          default: disable_grace_period
          selector:
            select:
              options:
              - label: Включить опцию льготного периода
                value: enable_grace_period
              - label: Отключить опцию льготного периода
                value: disable_grace_period
              sort: false
              multiple: false
              custom_value: false
        grace_period_timer_helper:
          name: Таймер helper для льготного периода
          description: "Выберите таймер helper, если вы включили льготный период выше.\n\n**Примечание** — При создании таймера helper отметьте флажок `Restore`, чтобы он сохранялся после перезапуска Home Assistant. Не задавайте длительность в самом таймере helper; время контролируется настройками ниже.\n\nЕсли используется таймер в опции **Таймер helper** выше, здесь необходимо использовать отдельный, независимый таймер.\n\nПодробнее о таймере helper — [нажмите здесь](https://community.home-assistant.io/t/688273/4)\n"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - timer
              multiple: false
              reorder: false
        grace_period_time_delay:
          name: Задержка льготного периода
          description: "Укажите, как долго длится льготный период после авто-выключения света из-за отсутствия движения. В течение этого времени при обнаружении движения свет включится автоматически. После окончания льготного периода свет необходимо включать вручную.\n"
          default:
            days: 0
            hours: 0
            minutes: 5
            seconds: 0
          selector:
            duration: {}
        saved_lights_input:
          name: Текстовый helper для сохранения включённых огней
          description: "При использовании более одной сущности-триггера без **Опции синхронизации**, выберите сущность `input_text` для хранения списка светов, которые сейчас включены.\n\nЕсли вы используете только одну сущность-триггер или **Опцию синхронизации**, можно оставить это поле пустым.\n"
          default: []
          selector:
            entity:
              filter:
              - domain:
                - input_text
              multiple: false
              reorder: false
    bypass_settings:
      name: Обход (Bypass)
      icon: mdi:cog-pause-outline
      collapsed: true
      input:
        include_bypass:
          name: Использовать опции обхода (опционально)
          description: "Выберите, хотите ли включить одну или несколько опций. Каждая опция определяет, как будет вести себя свет при включении обходного переключателя:\n\n  1 — Включает свет\n  2 — Выключает свет\n  3 — Сохраняет текущее состояние света\n\nЕсли вы включили опцию, обязательно укажите соответствующий обходной переключатель в поле ниже.\n\nПодробнее об использовании обхода — [нажмите здесь](https://community.home-assistant.io/t/688273/89)\n"
          default: []
          selector:
            select:
              options:
              - label: 1 — Включить обход — включить свет
                value: bypass_enabled_turn_on
              - label: 2 — Включить обход — выключить свет
                value: bypass_enabled_turn_off
              - label: 3 — Включить обход — сохранить текущее состояние света
                value: bypass_enabled_stop
              multiple: true
              sort: false
              custom_value: false
        bypass_lights_on:
          name: Переключатель обхода — включить свет
          description: "Выберите переключатели, которые будут включать свет, обходя триггер, и позволят управлять светом вручную. Обратите внимание, что эти сущности не должны быть включены в выбор «Триггер — Свет и выключатели».\n"
          default: []
          selector:
            entity:
              multiple: true
              reorder: false
        bypass_lights_off:
          name: Переключатель обхода — выключить свет
          description: "Выберите переключатели, которые будут выключать свет, обходя триггер, и позволят управлять светом вручную. Обратите внимание, что эти сущности не должны быть включены в выбор «Триггер — Свет и выключатели».\n"
          default: []
          selector:
            entity:
              multiple: true
              reorder: false
        bypass_lights_stop:
          name: Переключатель обхода — сохранить текущее состояние света
          description: "Выберите переключатели, которые будут сохранять текущее состояние света, обходя триггер, и позволят управлять светом вручную. Обратите внимание, что эти сущности не должны быть включены в выбор «Триггер — Свет и выключатели».\n"
          default: []
          selector:
            entity:
              multiple: true
              reorder: false
        bypass_time_delay:
          name: Обход — задержка времени
          description: "Используется только в двух конкретных сценариях, когда свет включён.\n\n  1. Когда выше выбрана опция 2 «Включить обход — выключить свет» и обход включён.\n  2. Когда все обходы выключены, а свет остаётся включён.\n\nВ обоих случаях автоматизация выключит свет после настроенной задержки.\n"
          default: 0
          selector:
            number:
              min: 0.0
              max: 10.0
              step: 0.25
              unit_of_measurement: minutes
              mode: slider
        include_bypass_auto_off:
          name: Использовать опцию авто-выключения обхода (опционально)
          description: "Включите эту опцию, если хотите, чтобы обход автоматически выключался через заданную задержку. Можно выбрать, для какой опции обхода применять авто-выключение:\n\n  1 — Включить свет\n  2 — Выключить свет\n  3 — Сохранить текущее состояние\n\nКаждая позиция соответствует одной из опций обхода, указанных выше.\n"
          default: []
          selector:
            select:
              options:
              - label: A — Включить авто-выключение для обхода опции 1 — включить свет
                value: bypass_auto_off_enabled_on
              - label: B — Включить авто-выключение для обхода опции 2 — выключить свет
                value: bypass_auto_off_enabled_off
              - label: C — Включить авто-выключение для обхода опции 3 — сохранить текущее состояние
                value: bypass_auto_off_enabled_stop
              multiple: true
              sort: false
              custom_value: false
        bypass_auto_off_delay:
          name: Авто-выключение обхода — задержка
          description: "Установите задержку авто-выключения обхода. Отсчёт начинается от последнего включённого обхода. Если одновременно включено несколько обходов, выключение одного при включённом другом также сбросит задержку.\n"
          default: 60
          selector:
            number:
              min: 1.0
              max: 240.0
              step: 1.0
              unit_of_measurement: minutes
              mode: slider
  source_url: https://gist.github.com/Blackshome/9f9785d7aa0ba7978fa6515a2d73d192
mode: restart
max_exceeded: silent
variables:
  trigger_input: !input trigger_input
  time_delay: !input time_delay
  include_timer_helper: !input include_timer_helper
  timer_helper: !input timer_helper
  include_trigger_sync: !input include_trigger_sync
  include_motion_trigger: !input include_motion_trigger
  motion_trigger: !input motion_trigger
  include_grace_period: !input include_grace_period
  grace_period_timer_helper: !input grace_period_timer_helper
  grace_period_time_delay: !input grace_period_time_delay
  saved_lights_input: !input saved_lights_input
  include_bypass: !input include_bypass
  bypass_lights_on: !input bypass_lights_on
  bypass_lights_off: !input bypass_lights_off
  bypass_lights_stop: !input bypass_lights_stop
  bypass_time_delay: !input bypass_time_delay
  include_bypass_auto_off: !input include_bypass_auto_off
  bypass_auto_off_delay: !input bypass_auto_off_delay
  trigger_entities_on: "{{ expand(trigger_input)\n    | selectattr('state', 'eq', 'on')\n    | map(attribute='entity_id')\n    | list }}\n"
  trigger_entities_off: "{{ expand(trigger_input)\n    | selectattr('state', 'eq', 'off')\n    | map(attribute='entity_id')\n    | list }}\n"
triggers:
- trigger: state
  id: t1
  entity_id: !input trigger_input
  from: 'off'
  to: 'on'
- trigger: state
  id: t2
  entity_id: !input trigger_input
  from: 'on'
  to: 'off'
- trigger: state
  entity_id: !input timer_helper
  to: idle
  id: t3
- trigger: state
  id: t4
  entity_id: !input motion_trigger
  from: 'off'
  to: 'on'
- trigger: state
  id: t5_on
  entity_id: !input bypass_lights_on
  from: 'off'
  to: 'on'
- trigger: state
  id: t5_off
  entity_id: !input bypass_lights_off
  from: 'off'
  to: 'on'
- trigger: state
  id: t5_stop
  entity_id: !input bypass_lights_stop
  from: 'off'
  to: 'on'
- trigger: state
  id: t6_on
  entity_id: !input bypass_lights_on
  from: 'on'
  to: 'off'
- trigger: state
  id: t6_off
  entity_id: !input bypass_lights_off
  from: 'on'
  to: 'off'
- trigger: state
  id: t6_stop
  entity_id: !input bypass_lights_stop
  from: 'on'
  to: 'off'
- trigger: homeassistant
  id: t7
  event: start
condition:
- condition: or
  conditions:
  - condition: and
    conditions:
    - condition: trigger
      id: t1
    - condition: state
      entity_id: !input trigger_input
      match: any
      state: 'on'
    - alias: Проверить, что сущность «ночные огни» включена
      condition: or
      conditions:
      - condition: and
        conditions:
        - '{{ include_grace_period == ''enable_grace_period'' }}'
        - condition: state
          entity_id: !input grace_period_timer_helper
          state: idle
      - condition: template
        value_template: '{{ include_grace_period == ''disable_grace_period'' }}'
      - condition: template
        value_template: '{{ trigger.to_state.context.parent_id is none }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t2
    - condition: state
      entity_id: !input trigger_input
      match: all
      state: 'off'
  - condition: and
    conditions:
    - condition: trigger
      id: t2
    - condition: template
      value_template: '{{include_trigger_sync == ''enable_trigger_sync''}}'
  - condition: and
    conditions:
    - condition: trigger
      id: t3
    - condition: template
      value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
    - condition: template
      value_template: '{{ expand(trigger_input) | selectattr(''state'', ''=='', ''on'') | list | count > 0 }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t4
    - '{{include_motion_trigger == ''enable_motion_trigger''}}'
    - alias: Проверить, включён ли льготный период
      condition: or
      conditions:
      - condition: and
        conditions:
        - '{{ expand(trigger_input) | selectattr(''state'', ''=='', ''on'') | list | count > 0 }}'
      - condition: and
        conditions:
        - condition: template
          value_template: '{{ include_grace_period == ''enable_grace_period'' }}'
        - condition: state
          entity_id: !input grace_period_timer_helper
          state: active
  - condition: and
    conditions:
    - condition: trigger
      id: t5_on
    - '{{ ''bypass_enabled_turn_on'' in include_bypass }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t5_off
    - '{{ ''bypass_enabled_turn_off'' in include_bypass }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t5_stop
    - '{{ ''bypass_enabled_stop'' in include_bypass }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t6_on
    - '{{ ''bypass_enabled_turn_on'' in include_bypass }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t6_off
    - '{{ ''bypass_enabled_turn_off'' in  include_bypass }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t6_stop
    - '{{ ''bypass_enabled_stop'' in include_bypass }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t7
    - condition: template
      value_template: '{{ expand(trigger_input) | selectattr(''state'', ''=='', ''on'') | list | count > 0 }}'
  - condition: and
    conditions:
    - condition: trigger
      id: t7
    - '{{ (''bypass_enabled_turn_on'' in include_bypass) or (''bypass_enabled_turn_off'' in include_bypass) or (''bypass_enabled_stop'' in include_bypass) }}'
    - '{{ (''bypass_auto_off_enabled_on'' in include_bypass_auto_off) or (''bypass_auto_off_enabled_off'' in include_bypass_auto_off) or (''bypass_auto_off_enabled_stop'' in include_bypass_auto_off) }}'
    - condition: or
      conditions:
      - condition: state
        entity_id: !input bypass_lights_on
        match: any
        state: 'on'
      - condition: state
        entity_id: !input bypass_lights_off
        match: any
        state: 'on'
      - condition: state
        entity_id: !input bypass_lights_stop
        match: any
        state: 'on'
- condition: or
  conditions:
  - '{{ include_bypass == [] }}'
  - condition: and
    conditions:
    - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
    - condition: state
      entity_id: !input bypass_lights_on
      state: 'off'
    - condition: state
      entity_id: !input bypass_lights_off
      state: 'off'
    - condition: state
      entity_id: !input bypass_lights_stop
      state: 'off'
  - condition: and
    conditions:
    - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
    - condition: state
      entity_id: !input bypass_lights_on
      state: 'off'
    - condition: state
      entity_id: !input bypass_lights_off
      state: 'off'
  - condition: and
    conditions:
    - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
    - condition: state
      entity_id: !input bypass_lights_on
      state: 'off'
    - condition: state
      entity_id: !input bypass_lights_stop
      state: 'off'
  - condition: and
    conditions:
    - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
    - condition: state
      entity_id: !input bypass_lights_off
      state: 'off'
    - condition: state
      entity_id: !input bypass_lights_stop
      state: 'off'
  - condition: and
    conditions:
    - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
    - condition: state
      entity_id: !input bypass_lights_on
      state: 'off'
  - condition: and
    conditions:
    - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
    - condition: state
      entity_id: !input bypass_lights_off
      state: 'off'
  - condition: and
    conditions:
    - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
    - condition: state
      entity_id: !input bypass_lights_stop
      state: 'off'
  - condition: trigger
    id:
    - t5_on
    - t5_off
    - t5_stop
    - t6_on
    - t6_off
    - t6_stop
    - t7
action:
- choose:
  - alias: Обход включён: применить опцию — включить свет
    conditions:
    - condition: trigger
      id: t5_on
    sequence:
    - choose:
      - alias: Если выбран льготный период
        conditions:
        - condition: template
          value_template: '{{include_grace_period == ''enable_grace_period''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
        - condition: state
          entity_id: !input grace_period_timer_helper
          state: active
        sequence:
        - alias: Отменить таймер helper льготного периода
          action: timer.cancel
          entity_id: !input grace_period_timer_helper
    - alias: Включить сущности
      action: homeassistant.turn_on
      entity_id: !input trigger_input
    - alias: Проверить настройки обхода и выполнить соответствующее действие
      if:
      - condition: or
        conditions:
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_on'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_on
            match: any
            state: 'on'
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_off'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_off
            match: any
            state: 'on'
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_stop'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_stop
            match: any
            state: 'on'
      then:
      - choose:
        - alias: Проверить, что таймер helper включён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
          sequence:
          - alias: Запустить таймер helper
            action: timer.start
            entity_id: !input timer_helper
            data:
              duration:
                minutes: !input bypass_auto_off_delay
          - alias: Дождаться завершения таймера авто-выключения обхода
            wait_for_trigger:
            - trigger: event
              event_type: timer.finished
              event_data:
                entity_id: !input timer_helper
        - alias: Проверить, что таймер helper отключён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
          sequence:
          - alias: Подождать задержку авто-выключения обхода
            delay:
              minutes: !input bypass_auto_off_delay
      - alias: Параллельные действия для авто-выключения обхода
        parallel:
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_auto_off_enabled_on'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (включить свет)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_on
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_auto_off_enabled_off'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (выключить свет)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_off
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_stop'' in include_bypass) and (''bypass_auto_off_enabled_stop'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (сохранить состояние)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_stop
      - stop: Остановить автоматизацию
      else:
      - choose:
        - alias: Проверить, что таймер helper включён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
          sequence:
          - alias: Отменить таймер helper
            action: timer.cancel
            entity_id: !input timer_helper
      - stop: Остановить автоматизацию
  - alias: Обход включён: применить опцию — выключить свет
    conditions:
    - condition: trigger
      id: t5_off
    sequence:
    - choose:
      - alias: Если выбран льготный период
        conditions:
        - condition: template
          value_template: '{{include_grace_period == ''enable_grace_period''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
        - condition: state
          entity_id: !input grace_period_timer_helper
          state: active
        sequence:
        - alias: Отменить таймер helper льготного периода
          action: timer.cancel
          entity_id: !input grace_period_timer_helper
    - choose:
      - alias: Прове��ить, включён ли какой-либо свет
        conditions:
        - condition: state
          entity_id: !input trigger_input
          match: any
          state: 'on'
        sequence:
        - choose:
          - alias: Проверить, что таймер helper включён
            conditions:
            - condition: template
              value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
            sequence:
            - alias: Обработка таймера helper при значении 0
              choose:
              - conditions: '{{ bypass_time_delay | float == 0 }}'
                sequence:
                - alias: Отменить таймер при значении 0
                  action: timer.cancel
                  entity_id: !input timer_helper
              default:
              - alias: Запустить таймер на заданные минуты
                action: timer.start
                entity_id: !input timer_helper
                data:
                  duration:
                    minutes: !input bypass_time_delay
            - alias: Дождаться завершения таймера обхода
              wait_for_trigger:
              - trigger: event
                event_type: timer.finished
                event_data:
                  entity_id: !input timer_helper
          - alias: Проверить, что таймер helper отключён
            conditions:
            - condition: template
              value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
            sequence:
            - alias: Подождать задержку обхода
              delay:
                minutes: !input bypass_time_delay
        - alias: Выключить сущности
          action: homeassistant.turn_off
          entity_id: !input trigger_input
    - alias: Проверить настройки обхода и выполнить соответствующее действие
      if:
      - condition: or
        conditions:
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_on'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_on
            match: any
            state: 'on'
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_off'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_off
            match: any
            state: 'on'
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_stop'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_stop
            match: any
            state: 'on'
      then:
      - choose:
        - alias: Проверить, что таймер helper включён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
          sequence:
          - alias: Запустить таймер helper
            action: timer.start
            entity_id: !input timer_helper
            data:
              duration:
                minutes: !input bypass_auto_off_delay
          - alias: Дождаться завершения таймера авто-выключения обхода
            wait_for_trigger:
            - trigger: event
              event_type: timer.finished
              event_data:
                entity_id: !input timer_helper
        - alias: Проверить, что таймер helper отключён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
          sequence:
          - alias: Подождать задержку авто-выключения обхода
            delay:
              minutes: !input bypass_auto_off_delay
      - alias: Параллельные действия для авто-выключения обхода
        parallel:
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_auto_off_enabled_on'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (включить свет)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_on
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_auto_off_enabled_off'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (выключить свет)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_off
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_stop'' in include_bypass) and (''bypass_auto_off_enabled_stop'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (сохранить состояние)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_stop
      - stop: Остановить автоматизацию
      else:
      - choose:
        - alias: Проверить, что таймер helper включён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
          sequence:
          - alias: Отменить таймер helper
            action: timer.cancel
            entity_id: !input timer_helper
      - stop: Остановить автоматизацию
  - alias: Обход включён: применить опцию — сохранить текущее состояние света
    conditions:
    - condition: trigger
      id: t5_stop
    sequence:
    - choose:
      - alias: Проверить, что таймер helper включён
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
        sequence:
        - choose:
          - alias: Если выбран льготный период
            conditions:
            - condition: template
              value_template: '{{include_grace_period == ''enable_grace_period''}}'
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            - condition: state
              entity_id: !input grace_period_timer_helper
              state: active
            sequence:
            - alias: Отменить таймер helper льготного периода
              action: timer.cancel
              entity_id: !input grace_period_timer_helper
      - alias: Проверить, что таймер helper отключён
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
        sequence:
        - choose:
          - alias: Если выбран льготный период
            conditions:
            - condition: template
              value_template: '{{include_grace_period == ''enable_grace_period''}}'
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            - condition: state
              entity_id: !input grace_period_timer_helper
              state: active
            sequence:
            - alias: Отменить таймер helper льготного периода
              action: timer.cancel
              entity_id: !input grace_period_timer_helper
    - alias: Проверить настройки обхода и выполнить соответствующее действие
      if:
      - condition: or
        conditions:
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_on'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_on
            match: any
            state: 'on'
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_off'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_off
            match: any
            state: 'on'
        - condition: and
          conditions:
          - alias: Проверить, включено ли авто-выключение обхода
            condition: template
            value_template: '{{ ''bypass_auto_off_enabled_stop'' in include_bypass_auto_off }}'
          - condition: state
            entity_id: !input bypass_lights_stop
            match: any
            state: 'on'
      then:
      - choose:
        - alias: Проверить, что таймер helper включён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
          sequence:
          - alias: Запустить таймер helper
            action: timer.start
            entity_id: !input timer_helper
            data:
              duration:
                minutes: !input bypass_auto_off_delay
          - alias: Дождаться завершения таймера авто-выключения обхода
            wait_for_trigger:
            - trigger: event
              event_type: timer.finished
              event_data:
                entity_id: !input timer_helper
        - alias: Проверить, что таймер helper отключён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
          sequence:
          - alias: Подождать задержку авто-выключения обхода
            delay:
              minutes: !input bypass_auto_off_delay
      - alias: Параллельные действия для авто-выключения обхода
        parallel:
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_auto_off_enabled_on'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (включить свет)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_on
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_auto_off_enabled_off'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (выключить свет)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_off
        - sequence:
          - choose:
            - conditions:
              - condition: template
                value_template: '{{ (''bypass_enabled_stop'' in include_bypass) and (''bypass_auto_off_enabled_stop'' in include_bypass_auto_off) }}'
              sequence:
              - alias: Выключить обход (сохранить состояние)
                action: homeassistant.turn_off
                entity_id: !input bypass_lights_stop
      - stop: Остановить автоматизацию
      else:
      - choose:
        - alias: Проверить, что таймер helper включён
          conditions:
          - condition: template
            value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
          sequence:
          - alias: Отменить таймер helper
            action: timer.cancel
            entity_id: !input timer_helper
      - stop: Остановить автоматизацию
- choose:
  - alias: Обход выключен: проверить, есть ли включённые огни
    conditions:
    - condition: trigger
      id:
      - t6_on
      - t6_off
      - t6_stop
    - condition: state
      entity_id: !input trigger_input
      match: any
      state: 'on'
    sequence:
    - choose:
      - alias: Проверить, что все обходы выключены, и условия (если включены)
        conditions:
        - condition: or
          conditions:
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_on
              state: 'off'
            - condition: state
              entity_id: !input bypass_lights_off
              state: 'off'
            - condition: state
              entity_id: !input bypass_lights_stop
              state: 'off'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_on
              state: 'off'
            - condition: state
              entity_id: !input bypass_lights_off
              state: 'off'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_on
              state: 'off'
            - condition: state
              entity_id: !input bypass_lights_stop
              state: 'off'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_off
              state: 'off'
            - condition: state
              entity_id: !input bypass_lights_stop
              state: 'off'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_on
              state: 'off'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_off
              state: 'off'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_stop
              state: 'off'
        sequence:
        - choose:
          - alias: Проверить, что таймер helper включён
            conditions:
            - condition: template
              value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
            sequence:
            - choose:
              - alias: Если выбран льготный период
                conditions:
                - condition: template
                  value_template: '{{include_grace_period == ''enable_grace_period''}}'
                - condition: template
                  value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
                sequence:
                - choose:
                  - alias: Проверить, что синхронизация триггера выключена и более одной сущности в триггере
                    conditions:
                    - condition: template
                      value_template: '{{include_trigger_sync == ''disable_trigger_sync''}}'
                    - condition: template
                      value_template: '{{ trigger_input | length > 1 }}'
                    sequence:
                    - action: input_text.set_value
                      target:
                        entity_id: '{{ saved_lights_input }}'
                      data:
                        value: '{{ trigger_entities_on | join('', '') }}'
                - choose:
                  - alias: Если таймер льготного периода активен
                    conditions:
                    - condition: state
                      entity_id: !input grace_period_timer_helper
                      state:
                      - active
                      - paused
                    sequence:
                    - alias: Отменить таймер helper льготного периода
                      action: timer.cancel
                      entity_id: !input grace_period_timer_helper
            - choose:
              - alias: Если выбрана синхронизация триггера
                conditions:
                - condition: template
                  value_template: '{{include_trigger_sync == ''enable_trigger_sync''}}'
                sequence:
                - alias: Включить сущности
                  action: homeassistant.turn_on
                  target:
                    entity_id: '{{ trigger_entities_off }}'
            - alias: Обработка таймера helper при значении 0
              choose:
              - conditions: '{{ bypass_time_delay | float == 0 }}'
                sequence:
                - alias: Отменить таймер при значении 0
                  action: timer.cancel
                  entity_id: !input timer_helper
              default:
              - alias: Запустить таймер на заданные минуты
                action: timer.start
                entity_id: !input timer_helper
                data:
                  duration:
                    minutes: !input bypass_time_delay
            - choose:
              - alias: Проверить, что датчик движения включён и в состоянии ON
                conditions:
                - condition: template
                  value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
                - condition: state
                  entity_id: !input motion_trigger
                  state: 'on'
                sequence:
                - alias: Поставить таймер helper на паузу
                  action: timer.pause
                  entity_id: !input timer_helper
                - alias: Подождать, пока датчик движения переключится в OFF
                  wait_for_trigger:
                    trigger: state
                    entity_id: !input motion_trigger
                    from: 'on'
                    to: 'off'
                - alias: Запустить таймер helper заново
                  action: timer.start
                  entity_id: !input timer_helper
                  data:
                    duration: !input time_delay
          - alias: Проверить, что таймер helper отключён
            conditions:
            - condition: template
              value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
            sequence:
            - choose:
              - alias: Если выбран льготный период
                conditions:
                - condition: template
                  value_template: '{{include_grace_period == ''enable_grace_period''}}'
                - condition: template
                  value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
                sequence:
                - choose:
                  - alias: Проверить, что синхронизация триггера выключена и более одной сущности в триггере
                    conditions:
                    - condition: template
                      value_template: '{{include_trigger_sync == ''disable_trigger_sync''}}'
                    - condition: template
                      value_template: '{{ trigger_input | length > 1 }}'
                    sequence:
                    - action: input_text.set_value
                      target:
                        entity_id: '{{ saved_lights_input }}'
                      data:
                        value: '{{ trigger_entities_on | join('', '') }}'
                - choose:
                  - alias: Если таймер льготного периода активен
                    conditions:
                    - condition: state
                      entity_id: !input grace_period_timer_helper
                      state:
                      - active
                      - paused
                    sequence:
                    - alias: Отменить таймер helper льготного периода
                      action: timer.cancel
                      entity_id: !input grace_period_timer_helper
            - choose:
              - alias: Если выбрана синхронизация триггера
                conditions:
                - condition: template
                  value_template: '{{include_trigger_sync == ''enable_trigger_sync''}}'
                sequence:
                - alias: Включить сущности
                  action: homeassistant.turn_on
                  target:
                    entity_id: '{{ trigger_entities_off }}'
            - choose:
              - alias: Проверить, что датчик движения включён и в состоянии ON
                conditions:
                - condition: template
                  value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
                - condition: state
                  entity_id: !input motion_trigger
                  state: 'on'
                sequence:
                - alias: Подождать, пока датчик движения переключится в OFF
                  wait_for_trigger:
                    trigger: state
                    entity_id: !input motion_trigger
                    from: 'on'
                    to: 'off'
            - alias: Подождать время, указанное в задержке обхода
              delay:
                minutes: !input bypass_time_delay
            - choose:
              - alias: Если выбран льготный период
                conditions:
                - condition: template
                  value_template: '{{include_grace_period == ''enable_grace_period''}}'
                - condition: template
                  value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
                sequence:
                - alias: Запустить таймер helper льготного периода
                  action: timer.start
                  entity_id: !input grace_period_timer_helper
                  data:
                    duration: !input grace_period_time_delay
            - alias: Выключить сущности
              action: homeassistant.turn_off
              target:
                entity_id: '{{ trigger_entities_on }}'
        - stop: Остановить автоматизацию
      - alias: Проверить, включены ли какие-либо обходы
        conditions:
        - condition: or
          conditions:
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_on
              match: any
              state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_off
              match: any
              state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_stop
              match: any
              state: 'on'
        sequence:
        - alias: Проверить настройки обхода и выполнить соответствующее действие
          if:
          - condition: or
            conditions:
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_on'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_off'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_stop'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          then:
          - choose:
            - alias: Проверить, что таймер helper включён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
              sequence:
              - alias: Запустить таймер helper
                action: timer.start
                entity_id: !input timer_helper
                data:
                  duration:
                    minutes: !input bypass_auto_off_delay
              - alias: Дождаться завершения таймера авто-выключения обхода
                wait_for_trigger:
                - trigger: event
                  event_type: timer.finished
                  event_data:
                    entity_id: !input timer_helper
            - alias: Проверить, что таймер helper отключён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
              sequence:
              - alias: Подождать задержку авто-выключения обхода
                delay:
                  minutes: !input bypass_auto_off_delay
          - alias: Параллельные действия для авто-выключения обхода
            parallel:
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_auto_off_enabled_on'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (включить свет)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_on
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_auto_off_enabled_off'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (выключить свет)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_off
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_stop'' in include_bypass) and (''bypass_auto_off_enabled_stop'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (сохранить состояние)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_stop
          - stop: Остановить автоматизацию
          else:
          - choose:
            - alias: Проверить, что таймер helper включён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
              sequence:
              - alias: Отменить таймер helper
                action: timer.cancel
                entity_id: !input timer_helper
          - stop: Остановить автоматизацию
  - alias: Обход выключен: проверить, все ли огни выключены
    conditions:
    - condition: trigger
      id:
      - t6_on
      - t6_off
      - t6_stop
    - condition: state
      entity_id: !input trigger_input
      match: all
      state: 'off'
    sequence:
    - choose:
      - alias: Проверить, включены ли какие-либо обходы
        conditions:
        - condition: or
          conditions:
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_on
              match: any
              state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_off
              match: any
              state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_stop
              match: any
              state: 'on'
        sequence:
        - alias: Проверить настройки обхода и выполнить соответствующее действие
          if:
          - condition: or
            conditions:
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_on'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_off'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_stop'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          then:
          - choose:
            - alias: Проверить, что таймер helper включён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
              sequence:
              - alias: Запустить таймер helper
                action: timer.start
                entity_id: !input timer_helper
                data:
                  duration:
                    minutes: !input bypass_auto_off_delay
              - alias: Дождаться завершения таймера авто-выключения обхода
                wait_for_trigger:
                - trigger: event
                  event_type: timer.finished
                  event_data:
                    entity_id: !input timer_helper
            - alias: Проверить, что таймер helper отключён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
              sequence:
              - alias: Подождать задержку авто-выключения обхода
                delay:
                  minutes: !input bypass_auto_off_delay
          - alias: Параллельные действия для авто-выключения обхода
            parallel:
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_auto_off_enabled_on'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (включить свет)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_on
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_auto_off_enabled_off'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (выключить свет)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_off
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_stop'' in include_bypass) and (''bypass_auto_off_enabled_stop'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (сохранить состояние)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_stop
          - stop: Остановить автоматизацию
          else:
          - choose:
            - alias: Проверить, что таймер helper включён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
              sequence:
              - alias: Отменить таймер helper
                action: timer.cancel
                entity_id: !input timer_helper
          - stop: Остановить автоматизацию
      - alias: Проверить, что таймер helper включён
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
        sequence:
        - alias: Отменить таймер helper
          action: timer.cancel
          entity_id: !input timer_helper
  - alias: Перезапуск Home Assistant
    conditions:
    - condition: trigger
      id: t7
    sequence:
    - choose:
      - alias: Проверить, включены ли какие-либо обходы
        conditions:
        - condition: or
          conditions:
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: or
              conditions:
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_on
              match: any
              state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_enabled_stop'' not in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_off
              match: any
              state: 'on'
          - condition: and
            conditions:
            - '{{ (''bypass_enabled_turn_on'' not in include_bypass) and (''bypass_enabled_turn_off'' not in include_bypass) and (''bypass_enabled_stop'' in include_bypass) }}'
            - condition: state
              entity_id: !input bypass_lights_stop
              match: any
              state: 'on'
        sequence:
        - alias: Проверить настройки обхода и выполнить соответствующее действие
          if:
          - condition: or
            conditions:
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_on'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_on
                match: any
                state: 'on'
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_off'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_off
                match: any
                state: 'on'
            - condition: and
              conditions:
              - alias: Проверить, включено ли авто-выключение обхода
                condition: template
                value_template: '{{ ''bypass_auto_off_enabled_stop'' in include_bypass_auto_off }}'
              - condition: state
                entity_id: !input bypass_lights_stop
                match: any
                state: 'on'
          then:
          - choose:
            - alias: Проверить, что таймер helper включён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
              sequence:
              - alias: Запустить таймер helper
                action: timer.start
                entity_id: !input timer_helper
                data:
                  duration:
                    minutes: !input bypass_auto_off_delay
              - alias: Дождаться завершения таймера авто-выключения обхода
                wait_for_trigger:
                - trigger: event
                  event_type: timer.finished
                  event_data:
                    entity_id: !input timer_helper
            - alias: Проверить, что таймер helper отключён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
              sequence:
              - alias: Подождать задержку авто-выключения обхода
                delay:
                  minutes: !input bypass_auto_off_delay
          - alias: Параллельные действия для авто-выключения обхода
            parallel:
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_turn_on'' in include_bypass) and (''bypass_auto_off_enabled_on'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (включить свет)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_on
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_turn_off'' in include_bypass) and (''bypass_auto_off_enabled_off'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (выключить свет)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_off
            - sequence:
              - choose:
                - conditions:
                  - condition: template
                    value_template: '{{ (''bypass_enabled_stop'' in include_bypass) and (''bypass_auto_off_enabled_stop'' in include_bypass_auto_off) }}'
                  sequence:
                  - alias: Выключить обход (сохранить состояние)
                    action: homeassistant.turn_off
                    entity_id: !input bypass_lights_stop
          - stop: Остановить автоматизацию
          else:
          - choose:
            - alias: Проверить, что таймер helper включён
              conditions:
              - condition: template
                value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
              sequence:
              - alias: Отменить таймер helper
                action: timer.cancel
                entity_id: !input timer_helper
          - stop: Остановить автоматизацию
    - choose:
      - alias: При рестарте HA: таймер включён и движение ON
        conditions:
        - '{{include_timer_helper == ''enable_timer_helper''}}'
        - '{{include_motion_trigger == ''enable_motion_trigger''}}'
        - condition: state
          entity_id: !input motion_trigger
          state: 'on'
        sequence:
        - choose:
          - alias: Проверить, что таймер на паузе
            conditions:
            - condition: state
              entity_id: !input timer_helper
              state: paused
            sequence:
            - alias: Поставить таймер helper на паузу
              action: timer.pause
              entity_id: !input timer_helper
            - alias: Подождать, пока датчик движения переключится в OFF
              wait_for_trigger:
                trigger: state
                entity_id: !input motion_trigger
                from: 'on'
                to: 'off'
            - alias: Запустить таймер helper заново
              action: timer.start
              entity_id: !input timer_helper
              data:
                duration: !input time_delay
          - alias: Проверить, что таймер в состоянии idle
            conditions:
            - condition: state
              entity_id: !input timer_helper
              state: idle
            sequence:
            - alias: Выключить сущности
              action: homeassistant.turn_off
              entity_id: !input trigger_input
      - alias: При рестарте HA: таймер включён и движение OFF
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
        - condition: state
          entity_id: !input motion_trigger
          state: 'off'
        sequence:
        - choose:
          - alias: Проверить, что таймер на паузе
            conditions:
            - condition: state
              entity_id: !input timer_helper
              state: paused
            sequence:
            - alias: Запустить таймер helper заново
              action: timer.start
              entity_id: !input timer_helper
              data:
                duration: !input time_delay
          - alias: Проверить, что таймер в состоянии idle
            conditions:
            - condition: state
              entity_id: !input timer_helper
              state: idle
            sequence:
            - alias: Выключить сущности
              action: homeassistant.turn_off
              entity_id: !input trigger_input
      - alias: При рестарте HA: таймер включён и опция движения отключена
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''disable_motion_trigger''}}'
        sequence:
        - choose:
          - alias: Проверить, что таймер на паузе
            conditions:
            - condition: state
              entity_id: !input timer_helper
              state: paused
            sequence:
            - alias: Запустить таймер helper заново
              action: timer.start
              entity_id: !input timer_helper
              data:
                duration: !input time_delay
          - alias: Проверить, что таймер в состоянии idle
            conditions:
            - condition: state
              entity_id: !input timer_helper
              state: idle
            sequence:
            - alias: Выключить сущности
              action: homeassistant.turn_off
              entity_id: !input trigger_input
      - alias: При рестарте HA: таймер отключён и движение ON
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
        - condition: state
          entity_id: !input motion_trigger
          state: 'on'
        sequence:
        - alias: Подождать, пока датчик движения переключится в OFF
          wait_for_trigger:
            trigger: state
            entity_id: !input motion_trigger
            from: 'on'
            to: 'off'
        - alias: Применить задержку времени
          delay: !input time_delay
        - alias: Выключить сущности
          action: homeassistant.turn_off
          entity_id: !input trigger_input
      - alias: При рестарте HA: таймер отключён и движение OFF
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
        - condition: state
          entity_id: !input motion_trigger
          state: 'off'
        sequence:
        - alias: Применить задержку времени
          delay: !input time_delay
        - alias: Выключить сущности
          action: homeassistant.turn_off
          entity_id: !input trigger_input
      - alias: При рестарте HA: таймер отключён и опция движения отключена
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''disable_motion_trigger''}}'
        sequence:
        - alias: Применить задержку времени
          delay: !input time_delay
        - alias: Выключить сущности
          action: homeassistant.turn_off
          entity_id: !input trigger_input
    - stop: Остановить автоматизацию
- choose:
  - alias: Триггер включён (свет включён вручную)
    conditions:
    - condition: trigger
      id:
      - t1
    sequence:
    - choose:
      - alias: Проверить, что таймер helper включён
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
        sequence:
        - choose:
          - alias: Если выбран льготный период
            conditions:
            - condition: template
              value_template: '{{include_grace_period == ''enable_grace_period''}}'
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            sequence:
            - choose:
              - alias: Проверить, что синхронизация триггера выключена и более одной сущности в триггере
                conditions:
                - condition: template
                  value_template: '{{include_trigger_sync == ''disable_trigger_sync''}}'
                - condition: template
                  value_template: '{{ trigger_input | length > 1 }}'
                sequence:
                - action: input_text.set_value
                  target:
                    entity_id: '{{ saved_lights_input }}'
                  data:
                    value: '{{ trigger_entities_on | join('', '') }}'
            - choose:
              - alias: Если таймер льготного периода активен
                conditions:
                - condition: state
                  entity_id: !input grace_period_timer_helper
                  state:
                  - active
                  - paused
                sequence:
                - alias: Отменить таймер helper льготного периода
                  action: timer.cancel
                  entity_id: !input grace_period_timer_helper
        - choose:
          - alias: Если выбрана синхронизация триггера
            conditions:
            - condition: template
              value_template: '{{include_trigger_sync == ''enable_trigger_sync''}}'
            sequence:
            - alias: Включить сущности
              action: homeassistant.turn_on
              target:
                entity_id: '{{ trigger_entities_off }}'
        - alias: Запустить таймер helper
          action: timer.start
          entity_id: !input timer_helper
          data:
            duration: !input time_delay
        - choose:
          - alias: Проверить, что датчик движения включён и в состоянии ON
            conditions:
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            - condition: state
              entity_id: !input motion_trigger
              state: 'on'
            sequence:
            - alias: Поставить таймер helper на паузу
              action: timer.pause
              entity_id: !input timer_helper
            - alias: Подождать, пока датчик движения переключится в OFF
              wait_for_trigger:
                trigger: state
                entity_id: !input motion_trigger
                from: 'on'
                to: 'off'
            - alias: Запустить таймер helper заново
              action: timer.start
              entity_id: !input timer_helper
              data:
                duration: !input time_delay
      - alias: Проверить, что таймер helper отключён
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
        sequence:
        - choose:
          - alias: Если выбран льготный период
            conditions:
            - condition: template
              value_template: '{{include_grace_period == ''enable_grace_period''}}'
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            sequence:
            - choose:
              - alias: Проверить, что синхронизация триггера выключена и более одной сущности в триггере
                conditions:
                - condition: template
                  value_template: '{{include_trigger_sync == ''disable_trigger_sync''}}'
                - condition: template
                  value_template: '{{ trigger_input | length > 1 }}'
                sequence:
                - action: input_text.set_value
                  target:
                    entity_id: '{{ saved_lights_input }}'
                  data:
                    value: '{{ trigger_entities_on | join('', '') }}'
            - choose:
              - alias: Если таймер льготного периода активен
                conditions:
                - condition: state
                  entity_id: !input grace_period_timer_helper
                  state:
                  - active
                  - paused
                sequence:
                - alias: Отменить таймер helper льготного периода
                  action: timer.cancel
                  entity_id: !input grace_period_timer_helper
        - choose:
          - alias: Если выбрана синхронизация триггера
            conditions:
            - condition: template
              value_template: '{{include_trigger_sync == ''enable_trigger_sync''}}'
            sequence:
            - alias: Включить сущности
              action: homeassistant.turn_on
              target:
                entity_id: '{{ trigger_entities_off }}'
        - choose:
          - alias: Проверить, что датчик движения включён и в состоянии ON
            conditions:
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            - condition: state
              entity_id: !input motion_trigger
              state: 'on'
            sequence:
            - alias: Подождать, пока датчик движения переключится в OFF
              wait_for_trigger:
                trigger: state
                entity_id: !input motion_trigger
                from: 'on'
                to: 'off'
        - alias: Подождать время, указанное в задержке времени
          delay: !input time_delay
        - choose:
          - alias: Если выбран льготный период
            conditions:
            - condition: template
              value_template: '{{include_grace_period == ''enable_grace_period''}}'
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            sequence:
            - alias: Запустить таймер helper льготного периода
              action: timer.start
              entity_id: !input grace_period_timer_helper
              data:
                duration: !input grace_period_time_delay
        - alias: Выключить сущности
          action: homeassistant.turn_off
          target:
            entity_id: '{{ trigger_entities_on }}'
  - alias: Триггер выключен (свет выключен вручную)
    conditions:
    - condition: trigger
      id: t2
    sequence:
    - choose:
      - alias: Проверить, что таймер helper включён, и отменить его
        conditions:
        - condition: state
          entity_id: !input trigger_input
          match: all
          state: 'off'
        sequence:
        - choose:
          - alias: Проверить, что таймер helper включён, и отменить его
            conditions:
            - condition: template
              value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
            sequence:
            - alias: Отменить таймер helper
              action: timer.cancel
              entity_id: !input timer_helper
      - alias: Проверить, что синхронизация триггера включена, и выключение вручную
        conditions:
        - condition: template
          value_template: '{{include_trigger_sync == ''enable_trigger_sync''}}'
        sequence:
        - alias: Выключить сущности
          action: homeassistant.turn_off
          target:
            entity_id: '{{ trigger_entities_on }}'
        - stop: Остановить автоматизацию
  - alias: Выключить сущности при завершении таймера helper
    conditions:
    - condition: trigger
      id: t3
    sequence:
    - choose:
      - alias: Если выбран льготный период
        conditions:
        - condition: template
          value_template: '{{include_grace_period == ''enable_grace_period''}}'
        - condition: template
          value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
        sequence:
        - alias: Запустить таймер helper льготного периода
          action: timer.start
          entity_id: !input grace_period_timer_helper
          data:
            duration: !input grace_period_time_delay
        - choose:
          - alias: Проверить, что синхронизация триггера выключена, и более одной сущности в триггере
            conditions:
            - condition: template
              value_template: '{{include_trigger_sync == ''disable_trigger_sync''}}'
            - condition: template
              value_template: '{{ trigger_input | length > 1 }}'
            sequence:
            - action: input_text.set_value
              data:
                entity_id: '{{ saved_lights_input }}'
                value: '{{ trigger_entities_on | join('', '') }}'
    - alias: Выключить сущности
      action: homeassistant.turn_off
      target:
        entity_id: '{{ trigger_entities_on }}'
  - alias: Обнаружено движение
    conditions:
    - condition: trigger
      id: t4
    sequence:
    - choose:
      - alias: Проверить, что таймер helper включён
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''enable_timer_helper''}}'
        sequence:
        - choose:
          - alias: Если выбран льготный период
            conditions:
            - condition: template
              value_template: '{{include_grace_period == ''enable_grace_period''}}'
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            - condition: state
              entity_id: !input grace_period_timer_helper
              state: active
            sequence:
            - choose:
              - alias: Проверить, что синхронизация триггера выключена и более одной сущности в триггере
                conditions:
                - condition: template
                  value_template: '{{include_trigger_sync == ''disable_trigger_sync''}}'
                - condition: template
                  value_template: '{{ trigger_input | length > 1 }}'
                sequence:
                - action: homeassistant.turn_on
                  target:
                    entity_id: '{% set entities = states(saved_lights_input) %} {% set cleaned_entities = entities.split('', '') %} {% set cleaned_entities = cleaned_entities | map(''trim'') | selectattr(''strip'', ''ne'', '''') | list %} {{ cleaned_entities }}\n\n'
              - alias: Проверить, что синхронизация триггера включена или всего одна сущность в триггере
                conditions:
                - condition: template
                  value_template: '{{ (include_trigger_sync == ''enable_trigger_sync'') or (trigger_input | length == 1) }}'
                sequence:
                - alias: Включить сущности
                  action: homeassistant.turn_on
                  target:
                    entity_id: '{{ trigger_entities_off }}'
            - alias: Отменить таймер helper льготного периода
              action: timer.cancel
              entity_id: !input grace_period_timer_helper
        - alias: Поставить таймер helper на паузу
          action: timer.pause
          entity_id: !input timer_helper
        - alias: Подождать, пока датчик движения переключится в OFF
          wait_for_trigger:
            trigger: state
            entity_id: !input motion_trigger
            from: 'on'
            to: 'off'
        - alias: Запустить таймер helper заново
          action: timer.start
          entity_id: !input timer_helper
          data:
            duration: !input time_delay
      - alias: Проверить, что таймер helper отключён
        conditions:
        - condition: template
          value_template: '{{include_timer_helper == ''disable_timer_helper''}}'
        sequence:
        - choose:
          - alias: Если выбран льготный период
            conditions:
            - condition: template
              value_template: '{{include_grace_period == ''enable_grace_period''}}'
            - condition: template
              value_template: '{{include_motion_trigger == ''enable_motion_trigger''}}'
            - condition: state
              entity_id: !input grace_period_timer_helper
              state: active
            sequence:
            - choose:
              - alias: Проверить, что синхронизация триггера выключена и более одной сущности в триггере
                conditions:
                - condition: template
                  value_template: '{{include_trigger_sync == ''disable_trigger_sync''}}'
                - condition: template
                  value_template: '{{ trigger_input | length > 1 }}'
                sequence:
                - action: homeassistant.turn_on
                  target:
                    entity_id: '{% set entities = states(saved_lights_input) %} {% set cleaned_entities = entities.split('', '') %} {% set cleaned_entities = cleaned_entities | map(''trim'') | selectattr(''strip'', ''ne'', '''') | list %} {{ cleaned_entities }}\n\n'
              - alias: Проверить, что синхронизация триггера включена или всего одна сущность в триггере
                conditions:
                - condition: template
                  value_template: '{{ (include_trigger_sync == ''enable_trigger_sync'') or (trigger_input | length == 1) }}'
                sequence:
                - alias: Включить сущности
                  action: homeassistant.turn_on
                  target:
                    entity_id: '{{ trigger_entities_off }}'
            - alias: Отменить таймер helper льготного периода
              action: timer.cancel
              entity_id: !input grace_period_timer_helper
        - alias: Подождать, пока датчик движения переключится в OFF
          wait_for_trigger:
            trigger: state
            entity_id: !input motion_trigger
            from: 'on'
            to: 'off'
        - alias: Подождать заданную задержку
          delay: !input time_delay
        - alias: Выключить сущности
          action: homeassistant.turn_off
          entity_id: !input trigger_input
